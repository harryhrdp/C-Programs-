#include<iostream>
#include<conio.h>
#include<string>
using namespace std;
main()
{
      string str;
      getline(cin,str);
      cout<<str;
      reverse(str.begin(),str.end());
      reversestr(str);
      cout<<str;
      getch();
      }
